// This is an empty config file to prevent Next.js from auto-detecting
module.exports = {
  // This is a static HTML site, no Next.js required
  // This file is only here to prevent auto-detection
  reactStrictMode: true,
  skipMiddlewareUrlNormalize: true,
  skipTrailingSlashRedirect: true,
} 